# Dependências adicionais para RAG Local + Memória Expandida
# Instale com: pip install -r requirements_rag.txt

# Embeddings locais (multilíngue)
sentence-transformers>=2.2.2
scikit-learn>=1.3.0

# Alternativas (escolha uma):
# - FAISS para busca vetorial ultra-rápida (especialmente bom para 8000 chunks)
faiss-cpu>=1.7.4

# Ou usar LanceDB para melhor performance com SQLite
lancedb>=0.1.0

# Processamento de áudio/transcrição
# (já pode estar nos requirements.txt, mas adicionado aqui para clareza)
librosa>=0.10.0
soundfile>=0.12.1

# Compressão de contexto (opcional, para otimizar tokens)
# llm-lingua>=0.1.0

# Monitoramento e logging (opcional)
prometheus-client>=0.17.0
